// vitest.config.ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',   // нужен jsdom, если дальше будут React-тесты
    globals: true,
  },
})
